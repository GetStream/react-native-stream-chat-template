import {
  STREAM_CHAT_API_KEY,
  STREAM_CHAT_USER_ID,
  STREAM_CHAT_USER_TOKEN,
} from 'react-native-dotenv';

export const chatApiKey = STREAM_CHAT_API_KEY;
export const chatUserId = STREAM_CHAT_USER_ID;
export const chatUserToken = STREAM_CHAT_USER_TOKEN;
